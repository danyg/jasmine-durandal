define(function() {
    var ctor = function () {
    	
        //The idea of this module is to explode to test if 
        //DurandalEnvironment can detect that an anunciate 
        //the error via jasmine
    };

    return ctor;
});